#include <iostream>
using namespace std;

int main() {
    int arr[5];
    
    for(int i = 0; i < 5; i++) {
        arr[i] = 1;
        cout << arr[i] << " ";
    }

    // another way to print the array in the given scenario

    // for(int i = 0; i < 5; i++) {
    //     cout << arr[i] << " ";
    // }

    return 0;
}